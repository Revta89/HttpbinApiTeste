[pytest]
minversion = 8.0
addopts = -q --alluredir=reports/allure
testpaths = tests
pythonpath = src
markers =
    response_formats: Tests validating response formats endpoints
    request_inspection: Tests validating request inspection endpoints
    dynamic_data: Tests generating and validating dynamic data
